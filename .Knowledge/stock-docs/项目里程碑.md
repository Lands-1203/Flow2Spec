# 项目里程碑

> **范围**：整个项目 / Flow2Spec 双仓里程碑同步  
> **生成时间**：2026-06-04  
> **索源**：req-docs：`.Knowledge/req-docs/*.md` 当前无 Markdown 文件；git：`git log --no-merges`、`git tag -l`、`package.json`（当前工作区版本 `3.1.1`，已提交 tag 至 `V3.0.19` / 发布提交至 `3.1.0`）；`.task`：`.task/todo.json`、`.task/active/lazy_loading_rule_optimization/task.md`、`.task/completed/20260515-cli_update_and_version/task.md`、`.task/completed/20260527-dual_repo_package_naming_rule/task.md`、`.task/completed/20260527-f2s_doc_add_multi_module_detection/task.md`、`.task/completed/20260528-negation_style_global_rule/task.md`、`.task/completed/20260528-readme_task_and_capabilities_update/task.md`；知识库主题：`config-precheck`、`f2s-task`、`f2s-req-plan`、`f2s-git-commit`、`flow2spec-milestones`、`flow2spec-presentations`、`skill-authoring`、`flow2spec-architecture`、`implement-tech-design`、`stock-docs-vs-req-docs`、`fallback-triage`（含 `manifest-routing.json`、`index.md`、对应 `topics/*.md` 与相关 matcher 分片）

## 总览

| 阶段 | 时间 | 一句话 |
| --- | --- | --- |
| M1 CLI 雏形与 OpenSpec 协作流 | 2026-02 ~ 2026-04 | Flow2Spec 从可安装 CLI 起步，早期围绕 OpenSpec / opsx 变更流组织 AI 协作。 |
| M2 f2s 需求与方案链路 | 2026-04 | `f2s-req-clarify`、`f2s-req-tech` 与技能化目录成形，开始从 OpenSpec 迁往自研 f2s 流程。 |
| M3 `.Knowledge` 机读路由 | 2026-05-08 | 引入 `.Knowledge`、`manifest-routing.json`、topics 与 matchers，确立 `match -> expand -> verify -> act` 的知识库消费链路。 |
| M4 对外演示与双语资料 | 2026-05-13 ~ 2026-05-14 | 新增公开演示稿、英文文档、GitHub Pages 同步脚本和 Cursor 插件提交结构。 |
| M5 CLI 运维命令与 req-plan 路由 | 2026-05-15 | 增加 `version` / `update` 命令，并把 `f2s-task`、`f2s-req-plan` 纳入包模板路由。 |
| M6 Codex 根入口与配置发现 | 2026-05-16 | `flow2spec init codex` 写入根 `AGENTS.md` 完整规则，`.codex/AGENTS.md` 退化为指针。 |
| M7 四源里程碑生成能力 | 2026-05-18 | 新增 `f2s-doc-milestone` 与项目里程碑模板，要求按 req-docs、git、`.task`、知识库主题生成阶段。 |
| M8 演示叙事与知识环表达 | 2026-05-18 ~ 2026-05-20 | 对外演示强化 Memory Coding 四环、知识环多层记忆与中英文页面同步。 |
| M9 文档链路与目录去业务化 | 2026-05-21 | 重组文档目录，明确项目知识库、`req-docs` / `stock-docs` 与 doc-arch / doc-final 链路边界。 |
| M10 命令速查与任务能力外显 | 2026-05-26 ~ 2026-05-28 | README、命令说明和英文 README 补齐任务清单、日常流程与能力描述。 |
| M11 文档沉淀规则补强 | 2026-05-27 ~ 2026-05-28 | 补齐 `f2s-doc-add` 多模块检测、双仓包名规则、终稿来源和全局否定式规则。 |
| M12 3.0.19 双仓模板对齐 | 2026-05-28 | 发布 `V3.0.19`，同步开源仓与内部仓 README、知识库和配置根模板。 |
| M13 知识工程规范 | 2026-06-03 | 增加 `skill-authoring`、`f2s-kb-addRules`、`f2s-topic-authoring` 与大功能拆分规则，统一知识创作口径。 |
| M14 主题分类与 init 合并校验 | 2026-06-03 | `topicMetadata` 支持 `primary` / `tags` / `confidence`，并配套 skill 重命名、旧目录清理和 init 合并校验。 |
| M15 更新检测与三端启动提醒 | 2026-06-04 | `updateCheck.enabled`、SessionStart hooks、自然日缓存与 Claude / Cursor / Codex 配置提醒分工落地。 |
| M16 f2s-git-commit 快捷提交 | 2026-06-04 | `f2s-git-commit` 支持快捷提交，仅跳过知识库覆盖检查，同时保留 diff、精确 add、hooks 与提交信息展示。 |

## M1 · CLI 雏形与 OpenSpec 协作流

- **交付**：CLI 具备可安装和初始化能力；早期围绕 OpenSpec / opsx 变更流组织需求、变更与执行；后续里程碑文档将该阶段定位为自研 f2s 路线前的产品雏形。
- **依据**：`git log --no-merges` 中 `009a9d6`（初始化）、`abd0c37`（版本更新）；既有 `Flow2Spec-项目里程碑.md` 的 M0 / M1；tag `V2.2.3` 之前的历史记录。

## M2 · f2s 需求与方案链路

- **交付**：需求澄清、技术方案生成与技能模板化链路成形；`f2s-req-clarify` / `f2s-req-tech` 成为后续 `implement-tech-design` 与 `req-docs` 方案输入的上游能力。
- **依据**：commit `b72cc0a`（全部转换为 skills）、`afa68f1`（修复需求澄清 skill）、`817fe49`（大改版本）、`95ab73d`（v2.2.0）；主题 `implement-tech-design`、`stock-docs-vs-req-docs`。

## M3 · `.Knowledge` 机读路由

- **交付**：引入 `.Knowledge` 目录、`manifest-routing.json`、`matchers/*.json` 与 `topics/*.md`；统一 f2s 规则进入 init / Codex / 技能模板；普通任务也要求先从路由清单进入渐进读取。
- **依据**：commit `594bc95`（引入 `.Knowledge` 与统一 f2s 规则，扩展 init / Codex 与技能模板）、`025e5c6`（升至 3.0.7，补齐 config 预检、KB 预检与 Karpathy 全端模板）；主题 `config-precheck`、`fallback-triage`、`flow2spec-architecture`。

## M4 · 对外演示与双语资料

- **交付**：新增 `presentations/flow2spec-intro-public/`、`presentations/flow2spec-intro-public-en/`、`sync-gh-pages.sh`、英文文档和 README 中英入口；开源仓补 Cursor Directory 插件结构与安装后引导规则。
- **依据**：commit `d37c198`、`975a8a7`、`f26e831`、`75d9822`、`b80c648`、`fa024ad`、`f03ac82`、`2ade402`、`515a653`；主题 `flow2spec-presentations`、`flow2spec-milestones`。

## M5 · CLI 运维命令与 req-plan 路由

- **交付**：CLI 增加 `version` / `update` 子命令；包模板补齐 `f2s-task` 与 `f2s-req-plan` 的 topic / matcher / dependency 路由；任务清单能力进入知识库与 README 叙事。
- **依据**：commit `f88613a`（新增 version 和 update 命令）、`40ac17e`（双仓同步 KB、里程碑与 f2s-req-plan）、`554fb8f`、`bbf71d2`；`.task/completed/20260515-cli_update_and_version/task.md`；主题 `f2s-task`、`f2s-req-plan`。

## M6 · Codex 根入口与配置发现

- **交付**：Codex 初始化改为根目录 `AGENTS.md` 自动发现，`.codex/AGENTS.md` 只保留指针；Flow2Spec 的 Codex 执行约束与知识库入口统一落到仓库根。
- **依据**：commit `508eef2`（根目录 AGENTS 自动发现，.codex 仅指针）、`d9b7f62`、`4fa1eee`；tag `V3.0.13`；主题 `config-precheck`。

## M7 · 四源里程碑生成能力

- **交付**：新增 `f2s-doc-milestone` 技能和 `.Knowledge/template/项目里程碑模版.md`；明确里程碑只能记录功能 / 能力变更，并强制覆盖 req-docs、git、`.task`、知识库主题四源。
- **依据**：commit `94acb8d`（新增 f2s-doc-milestone 并同步双仓里程碑 3.0.14）、`e9bc07d`（里程碑仅记功能变更并强化验证与落盘规则）；主题 `flow2spec-milestones`。

## M8 · 演示叙事与知识环表达

- **交付**：对外演示稿补充 Memory Coding 四环、知识环多层记忆、渐进式读取和匹配器表达；中英文 Pages 同步脚本持续对齐演示源。
- **依据**：commit `ad0619a`、`8a50dc5`、`7ef7768`、`0d3ed5b`、`e53d6e4`、`a718a42`、`ddaa019`、`167bbd9`、`86c6fe8`；主题 `flow2spec-presentations`。

## M9 · 文档链路与目录去业务化

- **交付**：重组文档目录并同步模板去业务化；明确产品仓 docs 与下游业务知识库的边界；`doc-arch` 禁止初稿直接驱动知识构建，强制走 `doc-final` 终稿链路。
- **依据**：commit `61a82e8`、`0daf81b`、`fc7c5a3`、`6c0623c`、`2d1b401`、`8c1ab8b`、`e372dff`；主题 `stock-docs-vs-req-docs`、`flow2spec-architecture`。

## M10 · 命令速查与任务能力外显

- **交付**：README 与命令说明补使用流程、命令速查、任务清单能力和日常开发流程；英文 README 同步能力描述，并移除会误导用户的 req-plan 表述。
- **依据**：commit `0443d85`、`f88b60c`、`4adab59`、`0c3e847`、`4d09a9b`、`cea5e27`、`491d6a5`；`.task/completed/20260528-readme_task_and_capabilities_update/task.md`。

## M11 · 文档沉淀规则补强

- **交付**：`f2s-doc-add` 增加多模块检测，避免不同功能模块内容被合并到同一输出；双仓包名规则写入里程碑和知识库；全局否定式规则沉淀到统一入口并由相关 skill 精简引用；终稿来源、初终稿边界和双仓命名规则补齐。
- **依据**：commit `807f9b1`、`f30ab9c`、`bdadcb6`、`dc9de11`；`.task/completed/20260527-f2s_doc_add_multi_module_detection/task.md`、`.task/completed/20260527-dual_repo_package_naming_rule/task.md`、`.task/completed/20260528-negation_style_global_rule/task.md`。

## M12 · 3.0.19 双仓模板对齐

- **交付**：发布 `V3.0.19`，开源仓与内部仓在 README、英文文档、知识库和配置根模板上保持同步；后续 6 月 3 日的知识工程规范以该版本带为基线继续演进。
- **依据**：commit `69b2526`（3.0.19 版本）、tag `V3.0.19`、commit `491d6a5`；既有 `项目里程碑.md` 与 `Flow2Spec-项目里程碑.md` 的 M13 前置阶段。

## M13 · 知识工程规范

- **交付**：新增 `skill-authoring` topic 与 matcher，统一 `f2s-*` SKILL 骨架、标题命名和完成后自检；新增 `f2s-rule-capture` 后再重命名为 `f2s-kb-addRules`，用于把用户口述规则沉淀进知识库；新增 `f2s-topic-authoring` 创作侧准则，覆盖 topic 命名、骨架、`topicDependencies` 判定、DAG 最小化和大功能拆分建议。
- **依据**：commit `f8ea24b`、`f37c282`、`ab0e57b`、`a0b669e`、`ee0864d`；主题 `skill-authoring`、`flow2spec-architecture`；matcher `m-skill-authoring.json`。

## M14 · 主题分类与 init 合并校验

- **交付**：`manifest-routing.json` 新增 `topicMetadata`，用 `primary`、`tags`、`confidence` 表达主题治理和阅读预期，不参与 matcher 命中；`init` 支持 metadata 合并、校验、模板优先覆盖；`copySkills` 自动清理旧 skill 目录，并限制在 `f2s-` 前缀和已知旧名范围内，避免误删用户自定义 skill；6 个 skill 名称完成 kb-/doc- 前缀规范化。
- **依据**：commit `e81148c`、`22f7049`、`6e1ae6d`、`d7dcebb`、`9188c9b`、`ddaa6ff`、`3455ee5`；`package.json` 发布提交 `3.1.0`；主题 `flow2spec-architecture`、`skill-authoring`。

## M15 · 更新检测与三端启动提醒

- **交付**：新增 `updateCheck.enabled` 配置，版本检测从 hook 执行演进到 SessionStart 触发；版本比较改为 `.Knowledge/manifest-routing.json` 的模板版本与 npm latest 对比；自然日缓存落到 `.Knowledge/update-check.json`；Claude 拆分 `f2s-config-session.js` 与 `f2s-config-inject.js`，SessionStart 输出配置摘要，PreToolUse 保留技能前读取提醒；Cursor 通过 `hooks.json` SessionStart 注入 `additional_context`；Codex 写入 `.codex/hooks.json` 与 `hookSpecificOutput.additionalContext`。
- **依据**：commit `fddb212`、`c5624e5`、`1630475`、`5884ecb`、`08012ec`、`002f608`、`20a0c6e`、`98a3789`、`f37543c`；主题 `config-precheck`；matcher `m-f2s-config-precheck.json`。

## M16 · f2s-git-commit 快捷提交

- **交付**：`f2s-git-commit` 新增快捷提交模式：只有用户明确说“快捷提交 / 快速提交 / quick commit”时，跳过步骤 2 的知识库覆盖检查；仍必须读取 diff、检查冲突标记、展示提交信息首行、精确 `git add <文件列表>`、正常执行 hooks，且禁止自动 pull / push、禁止 `git add -A`、禁止 `--no-verify`；知识库同步新增 `f2s-git-commit` topic 与 matcher。
- **依据**：commit `a284cbc`；主题 `f2s-git-commit`；matcher `m-f2s-git-commit.json`；README 与命令说明同步更新。

## 待确认

- `.Knowledge/req-docs/` 当前无 Markdown 文件，本次无法从需求 / 技术方案源补充独立需求锚点。
- `.task/active/lazy_loading_rule_optimization/` 记录的是 2026-06-03 规则懒加载优化待办，尚未完成归档；本次只在待确认记录，不作为 Mx 功能交付。
- 当前工作区 `package.json` 已从 `3.1.0` 改为 `3.1.1`，但 `git log --no-merges` 与 `git tag -l` 尚未出现对应发布提交 / tag；本次不把 `3.1.1` 写成已发布里程碑。
